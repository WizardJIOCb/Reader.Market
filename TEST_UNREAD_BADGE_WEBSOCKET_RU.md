# Отладка обновления счётчика непрочитанных сообщений в реальном времени

## Проблема
Счётчик непрочитанных сообщений не обновляется в списке пользователей, когда получатель получает сообщение, но у него не открыт чат с отправителем.

## Внесённые изменения

### Логирование на бэкенде (`server/routes.ts`)

Добавлено подробное цветное логирование для отслеживания WebSocket событий:

1. **При отправке сообщения** (строка ~2287):
   - ID отправителя, ID получателя, ID диалога
   - Названия комнат (`conversation:${id}` и `user:${recipientId}`)
   - Данные уведомления
   - **Проверка количества клиентов в комнате получателя**
   - Предупреждение, если к комнате получателя никто не подключен

2. **При подключении пользователя** (строка ~224):
   - Логирование подключения
   - Логирование присоединения к личной комнате `user:${userId}`
   - Список всех комнат, в которых находится сокет

### Логирование на фронтенде

1. **Подключение сокета** (`client/src/lib/socket.ts`, строка ~48):
   - Зелёный лог при успешном подключении
   - ID сокета
   - Уведомление об автоматическом присоединении к личной комнате

2. **Слушатель уведомлений** (`client/src/pages/Messages.tsx`, строка ~265):
   - Фиолетовые логи при получении уведомления
   - Полные данные события
   - Зелёный лог при загрузке диалогов
   - Оранжевое предупреждение, если тип уведомления не `new_message`

## Как протестировать

### Шаг 1: Перезапустить сервер

Изменения бэкенда требуют перезапуска:

```powershell
# В PowerShell
Stop-Process -Name node -Force
npm run dev
```

### Шаг 2: Открыть два окна браузера

**Окно 1 - Получатель (текущий пользователь)**
1. Открыть http://localhost:3001/messages
2. Открыть консоль DevTools (F12)
3. **НЕ ОТКРЫВАТЬ конкретный чат** - остаться на списке диалогов
4. Следить за логами:
   - `[SOCKET.IO] ✅ WebSocket connected` (зелёный)
   - `[SOCKET.IO] Socket ID: xxxxx`
   - `[NOTIFICATION LISTENER] ✅ Listener registered successfully` (зелёный)

**Окно 2 - Отправитель (WizardJIOCb или другой пользователь)**
1. Войти под другим пользователем
2. Открыть http://localhost:3001/messages
3. Открыть чат с текущим пользователем (из Окна 1)
4. Отправить сообщение

### Шаг 3: Проверить логи

**В консоли Окна 1 (Получатель)** должно появиться:
```
[NOTIFICATION LISTENER] 🔔 Notification received!  (фиолетовый)
[NOTIFICATION LISTENER] Event data: {
  "type": "new_message",
  "conversationId": "...",
  "senderId": "..."
}
[NOTIFICATION LISTENER] ✅ Type is new_message - will fetch conversations (зелёный)
Fetching conversations...
```

**В терминале сервера** должно появиться:
```
[WEBSOCKET] 📡 Emitting WebSocket events for new message (пурпурный)
[WEBSOCKET] Sender: <sender-id>, Recipient: <recipient-id> (пурпурный)
[WEBSOCKET] Conversation ID: <conv-id> (пурпурный)
[WEBSOCKET] Emitting 'message:new' to room: conversation:xxxxx (голубой)
[WEBSOCKET] ✅ Emitting 'notification:new' to room: user:xxxxx (зелёный)
[WEBSOCKET] Notification data: {"type":"new_message","conversationId":"...","senderId":"..."} (зелёный)
[WEBSOCKET] 👥 Number of clients in room 'user:xxxxx': 1 (жёлтый)
[WEBSOCKET] ✅ Event sent to 1 client(s) (зелёный)
```

### Ожидаемые результаты

✅ **Если всё работает правильно:**
- Фиолетовые логи уведомлений появляются в консоли Окна 1
- Сервер показывает 1 или больше клиентов в комнате получателя
- Счётчик на значке обновляется автоматически в списке диалогов
- Вызывается API для обновления списка диалогов

❌ **Если не работает:**

**Проблема A: Нет фиолетовых логов в Окне 1**
- Сервер показывает 0 клиентов в комнате получателя
- **Причина**: WebSocket не подключен или пользователь не в личной комнате
- **Проверить**: Искать зелёный лог `[SOCKET.IO] ✅ WebSocket connected`

**Проблема B: Фиолетовые логи есть, но значок не обновляется**
- **Причина**: Проблема с обновлением состояния React или API не возвращает обновлённый счётчик
- **Проверить**: Искать `Fetching conversations...` и проверить ответ API

**Проблема C: Сервер показывает 0 клиентов**
- **Причина**: Пользователь отключён или аутентификация не прошла
- **Проверить**: Искать `[WEBSOCKET] 🔗 User xxxxx connected` в логах сервера

## Команды для отладки

### Проверить работу Socket.IO в консоли браузера:
```javascript
// В консоли DevTools Окна 1
window.io  // Должен показать клиент Socket.IO
```

### Вручную обновить список диалогов:
```javascript
// В консоли DevTools Окна 1
fetch('http://localhost:5001/api/conversations', {
  headers: {
    'Authorization': 'Bearer ' + localStorage.getItem('authToken')
  }
}).then(r => r.json()).then(console.log)
```

### Проверить статус WebSocket подключения:
```javascript
// В консоли DevTools Окна 1
console.log('Socket connected:', window.__socket?.connected)
```

## Изменённые файлы

1. `server/routes.ts` - Строки 2287-2320 (логирование WebSocket)
2. `server/routes.ts` - Строки 224-236 (логирование подключения)
3. `client/src/lib/socket.ts` - Строки 48-62 (обработчики событий подключения)
4. `client/src/pages/Messages.tsx` - Строки 265-290 (глобальный слушатель уведомлений)

## Что делать дальше, если проблема остаётся

1. **Проверить базу данных бэкенда**: Убедиться, что `unreadCount` рассчитывается правильно

2. **Проверить вкладку Network**: Проверить WebSocket подключение в DevTools Network
   - Искать соединение `ws://` или `wss://`
   - Проверить ответ "101 Switching Protocols"

3. **Проверить CORS настройки**: Убедиться, что Socket.IO разрешает подключения с фронтенда
