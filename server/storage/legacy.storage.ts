import type { DB } from "./db";

// Placeholder for legacy storage methods that will be gradually migrated
export function createLegacyStorage(db: DB) {
  // This is a temporary placeholder to prevent import errors
  // Actual legacy methods will be moved here from the original storage.ts file
  return {};
}